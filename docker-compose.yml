version: '3.8'

services:
  web-cmt:
    build: .
    container_name: tiktok-comments-web
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    volumes:
      - ./downloads:/app/downloads
